{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "no-restricted-imports": ["error", {
      "paths": [{
        "name": "@/lib/errorHandler",
        "message": "Use @/lib/error-handler (kebab-case) instead — errorHandler.ts (camelCase) is dead code and must not be reintroduced."
      }]
    }]
  }
}
